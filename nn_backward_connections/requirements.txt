# Pinned to the conda `allen` environment that produced the reproduction numbers.
# Install path (preferred): recreate via environment.yml. This file documents the
# exact versions and supports `pip install -r requirements.txt` into a py3.9 env.
torch==2.8.0
numpy==1.23.5
scipy==1.10.1
networkx==3.2.1
pandas==1.5.3
PyYAML==6.0.2
nbformat==5.10.4
matplotlib==3.9.4
pytest>=7,<9
